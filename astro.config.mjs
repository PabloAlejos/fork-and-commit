import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://PabloAlejos.github.io',
  base: '/fork-and-commit', // Solo si es un repo de proyecto, no si es tu-usuario.github.io
});